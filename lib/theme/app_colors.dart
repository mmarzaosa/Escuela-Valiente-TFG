import 'package:flutter/material.dart';

class AppColors {
  // Colores Principales
  static const Color darkBlue = Color(0xFF1A4D8F);
  static const Color orangeMain = Color(0xFFFF9430);
  static const Color orangeGradientEnd = Color(0xFFFF6100);
  
  // Colores de Fondo
  static const Color backgroundCreme = Color.fromRGBO(255, 247, 238, 1);
  static const Color whiteTranslucent = Color.fromRGBO(255, 255, 255, 0.8);
  
  // Colores de Estado
  static const Color errorRed = Color(0xFFC70101);
  static const Color successGreen = Colors.green;
  
  // Colores de Texto
  static const Color textDark = Color.fromRGBO(60, 60, 60, 1);
  static const Color textLight = Colors.white;
}